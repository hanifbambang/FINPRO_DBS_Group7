# PostgreSQL Connection string (e.g. postgres://postgres:postgres@localhost:5432/pokedex)
DATABASE_URL=
# Redis Connection string (e.g. redis://default:password@host:port)
REDIS_URL=
# Secret for JWT signing
JWT_SECRET=
